!!! 5
%html(lang="en")
  %head
    %meta(charset="utf-8")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) : "Livecomments"
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
    = javascript_include_tag "jquery","http://html5shim.googlecode.com/svn/trunk/html5.js", "http://js.pusher.com/1.12/pusher.min.js"
    = stylesheet_link_tag "application", :media => "all"

  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container-fluid
          %a.btn.btn-navbar(data-target=".nav-collapse" data-toggle="collapse")
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.brand(href="/") Livecomments
          .container.nav-collapse
            %ul.nav
              %li= link_to "About", about_comments_path

    .container-fluid
      #notifications_place
      .row-fluid
        -if @comments_quantity
          .span3
            .well.sidebar-nav
              %ul.nav.nav-list
                %li.nav-header We are growing
                %li Commetns Quantity:
                %li
                  #com_quantity=@comments_quantity rescue 0
        .span9
          = yield

      %footer
        %p
          &copy; Livecomments
          =Time.now.year
    /
      Javascripts
      \==================================================
    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"

